// import contractInstance from "../VoteContractHelper";

// export const getOwner = async (): Promise<string | null> => {
//   try {
//     const ownerAddress = await contractInstance.methods.owner().call();
//     return typeof ownerAddress === "string" ? ownerAddress : null;
//   } catch (error) {
//     console.error("Error fetching contract owner:", error);
//     return null;
//   }
// };

// /**The owner typically refers to the Ethereum address of the person or account
//  * that deployed or administrates the contract */
